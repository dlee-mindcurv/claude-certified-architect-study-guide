# Project Standards

<!-- TODO Step 1: Define universal project standards for Claude Code.
     This CLAUDE.md is the project-level configuration that applies to ALL
     interactions in this repository. -->

## Code Style

<!-- TODO: Add code style conventions. Include:
     - Module system (ES modules with import/export)
     - Naming conventions (camelCase for variables/functions, PascalCase for classes)
     - File naming (kebab-case.js)
     - Semicolons, quotes, indentation preferences -->

## Testing Requirements

<!-- TODO: Add testing standards. Include:
     - Test framework (node:test or vitest)
     - Minimum coverage expectations
     - Test file naming convention (*.test.js)
     - What must be tested (public APIs, error paths, edge cases) -->

## Error Handling

<!-- TODO: Add error handling standards. Include:
     - Always use structured error responses (errorCategory, isRetryable, message)
     - Never swallow errors silently
     - Log errors with sufficient context for debugging
     - Use try/catch around async operations -->

## Git Conventions

<!-- TODO: Add git standards. Include:
     - Commit message format (conventional commits: feat:, fix:, refactor:, etc.)
     - Branch naming (feature/xxx, fix/xxx)
     - PR description requirements -->

## Import Ordering

<!-- TODO: Add import ordering rules. Include:
     1. Node.js built-in modules
     2. Third-party packages
     3. Local absolute imports
     4. Local relative imports
     Each group separated by a blank line -->
