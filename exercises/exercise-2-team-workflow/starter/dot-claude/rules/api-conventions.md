---
glob: "src/api/**/*.{js,ts}"
---

<!-- TODO Step 2: Add rules that apply ONLY to API route files.

These rules are automatically applied when Claude Code edits files matching
the glob pattern above.

Add rules for:
- Request validation (validate all inputs at the boundary)
- Error response format (consistent JSON error structure)
- HTTP status codes (when to use 400 vs 422 vs 500)
- Authentication/authorization checks
- Logging requirements for API endpoints
-->
