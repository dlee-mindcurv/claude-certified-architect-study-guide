---
glob: "**/*.test.{js,ts}"
---

<!-- TODO Step 2: Add rules that apply ONLY to test files.

These rules are automatically applied when Claude Code edits files matching
the glob pattern above.

Add rules for:
- Test structure (describe/it blocks, clear test names)
- Assertion style (use strict equality, avoid loose truthy checks)
- Mock patterns (how to mock external dependencies)
- Test isolation (each test should be independent, no shared mutable state)
- Edge cases that must be covered (null inputs, empty arrays, error paths)
-->
